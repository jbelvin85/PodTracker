# backend/.env.example
# The Architect: Backend service blueprint.
# This file is managed by the root .env and docker-compose.
# For local development outside of Docker, copy this to .env and configure.

DATABASE_URL="postgresql://admin:password@localhost:5432/podtracker?schema=public"
JWT_SECRET=your-super-secret-key
