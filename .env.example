# .env.example
# This file serves as a template for your environment variables for the PodTracker project.
# IMPORTANT: Copy this file to a new file named .env in the same directory.
# The .env file is ignored by Git and should contain your sensitive credentials.
# Fill in the values in your .env file.

# -----------------------------------------------------------------------------
# Docker Compose Variables
# These variables are used by Docker Compose to configure the overall project.
# -----------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=podtracker

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# These variables configure the PostgreSQL database service.
# -----------------------------------------------------------------------------
POSTGRES_USER=admin
POSTGRES_PASSWORD=YOUR_PASSWORD_HERE
POSTGRES_DB=podtracker

# -----------------------------------------------------------------------------
# Backend Service Configuration
# These variables are consumed by the Node.js backend application.
# -----------------------------------------------------------------------------
# Database URL for the backend to connect to PostgreSQL.
# When running with Docker Compose, 'db' is the service name for the database.
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public"
# Secret key for JSON Web Token (JWT) authentication.
JWT_SECRET=YOUR_SECRET_KEY_HERE
# Node.js environment mode (e.g., development, production).
NODE_ENV=development
# Port on which the backend server will listen.
BACKEND_PORT=3001

# -----------------------------------------------------------------------------
# Frontend Service Configuration (Vite)
# These variables are consumed by the React frontend application via Vite.
# IMPORTANT: Frontend variables MUST be prefixed with VITE_ to be exposed to the browser.
# -----------------------------------------------------------------------------
# Base URL for the backend API.
# When running with Docker Compose, 'backend' is the service name for the backend.
VITE_API_BASE_URL=http://localhost:3001
# Port on which the frontend development server will listen.
VITE_FRONTEND_PORT=8000