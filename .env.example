# --- General ---
# These are the default ports. You can change them if they conflict with other services.
FRONTEND_PORT=5173
BACKEND_PORT=3001
DB_PORT=5432
TEST_DB_PORT=5433

# --- Database ---
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=podtracker

# --- Backend ---
# These URLs are used by Prisma and the backend service.
# The host 'db' and 'test-db' refer to the service names in docker-compose.yml.
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public"
TEST_DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@test-db:5432/podtracker_test?schema=public"

# --- Security ---
# A strong, random secret for signing JSON Web Tokens.
# Generate one with